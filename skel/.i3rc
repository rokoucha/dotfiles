######################
#.i3rc for `hostname`#
# atnanasi/dotfiles  #
######################

# start Thinkpad OSD daemon
#if [ -x /usr/bin/tpb ] && [ -w /dev/nvram ] && [ -r /dev/nvram ] ; then
#	/usr/bin/tpb -d
#fi

# set im
#export GTK_IM_MODULE='fcitx'
#export QT_IM_MODULE='fcitx'
#export XMODIFIERS='@im=fcitx'
#fcitx-autostart

# start applet for blueman
#blueman-applet &

# start sync daemon for MEGA
#megasync -&

# start compsit manager(WARNING: THIS APPLICATIONS COURSE BUG!)
#compton -b &
